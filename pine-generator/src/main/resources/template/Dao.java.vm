package ${package}.${moduleName}.dao;

import java.util.List;
import java.util.Map;

import ${package}.${moduleName}.entity.${className};

/**
 * ${comments}
 * @motto ${motto}
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Dao {

    int deleteById(Long id);

    int insertSelective(${className} record);

    ${className} selectByPrimaryKey(Long id);

    int updateByPrimaryKeySelective(${className} record);

    List<${className}> queryAll${className}(${className} record);

    int delete(Long id);

    int deleteByIdList(String[] record);

    ${className} query${className}(${className} record);

}
